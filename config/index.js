export const config = {
    dellaCookie: process.env.DELLA_COOKIE,
    dellaUrl: process.env.DELLA_URL,
    telegramBotToken: process.env.TELEGRAM_BOT_TOKEN,
    telegramGroupId: process.env.TELEGRAM_GROUP_ID
}